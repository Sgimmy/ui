import { createAction } from '@reduxjs/toolkit';
import { Actions } from '../../constants/actions';

export const clearStateReduxSendArticleAction = createAction(
  Actions.clearStateReduxSendArticle
);
