export enum SendArticleType {
  title = 'title',
  description = 'description',
  tags = 'tags',
  source = 'source',
}

export enum PopupMessageType {
  error = 'error',
  success = 'success',
  message = 'message',
}
